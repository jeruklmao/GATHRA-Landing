# Visual review captures

These screenshots are generated by `tests/visual-review.spec.ts` from the static preview. The
browser viewports are:

- `home-mobile-375x812.png`: 375 × 812 px viewport.
- `home-tablet-768x1024.png`: 768 × 1024 px viewport.
- `home-desktop-1440x900.png`: 1440 × 900 px viewport.

They are review artifacts only and are not copied into the published site.
